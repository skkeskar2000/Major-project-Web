import 'package:flutter/material.dart';

class ManagerFillFormWidget extends StatelessWidget {
  const ManagerFillFormWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
